#include <bits/stdc++.h>
using namespace std;

vector<int> nextGreater(vector<int> &arr){
    vector<int> res = {};
    int size = arr.size();
    for(int i = 0; i < size; i++){
        res.push_back(i);
    }
    for(int i = 0; i < size; i++){
        for(int j = i + 1; j < size; j++){
            
        }
    }
}